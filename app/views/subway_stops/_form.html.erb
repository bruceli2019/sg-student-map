<%= form_with(model: subway_stop, local: true) do |form| %>
  <% if subway_stop.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(subway_stop.errors.count, "error") %> prohibited this subway_stop from being saved:</h2>

      <ul>
      <% subway_stop.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :subway_stop_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
